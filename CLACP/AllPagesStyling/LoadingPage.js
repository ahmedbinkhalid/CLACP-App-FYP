import EStyleSheet from 'react-native-extended-stylesheet';

export default styles = EStyleSheet.create({
    loadingPage : {
        bcakgroundColor : "#1a0000", 
        height : '20rem',
        width : '20rem'
    }
  });